server:
  port: 0

spring:
  application:
    name: user-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      fail-fast: false

eureka:
  client:
    enabled: false
